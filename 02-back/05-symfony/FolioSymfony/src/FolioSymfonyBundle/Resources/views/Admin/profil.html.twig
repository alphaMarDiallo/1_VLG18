{% extends 'adminLayout.html.twig' %}

{% block content %}

    <div class="container">
        <div class="row">
            <div class="col-lg-6 mt-5">
                 <img src="{{asset('/photo/avatar.jpg')}}" class="rounded-circle" alt="Responsive image">     
            </div>
            <div class="col-lg-6 mt-5">
            {% for user in users %}
                <h3> Bonjour {{user.upseudo}} </h3>
            {% endfor %}
            </div>
        </div>
    </div>

{% endblock %}