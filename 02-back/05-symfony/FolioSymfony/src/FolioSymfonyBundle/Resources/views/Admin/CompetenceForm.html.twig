{% extends 'adminLayout.html.twig' %}

{# {% block title %}{{ title }}{% endblock %} #}

{% block content %}
    <h1>{{ title }}</h1>

      {{form_start(competenceForm)}}
        {{form_errors(competenceForm)}}
            <div class="row">
                <div class="col mt-5">
                    <div class="form-group">
                        {{form_row(competenceForm.cptechnology,    {'attr' : {'class' : 'form-control', 'placeholder' : 'quellanguage ? '} } )}}
                    </div>
                    <div class="form-group">
                        {{form_row(competenceForm.cplevel,     {'attr' : {'class' : 'form-control', 'placeholder' : 'niveau de maitrise'} } )}}
                    </div>
                    <input type="submit" class=" alert-primary" value="Envoyer">
                </div>

            </div>
        {{form_rest(competenceForm)}}
        {{form_end(competenceForm)}}

{% endblock %}